import { FontSizeColorDirective } from './font-size-color.directive';

describe('FontSizeColorDirective', () => {
  it('should create an instance', () => {
    const directive = new FontSizeColorDirective();
    expect(directive).toBeTruthy();
  });
});
